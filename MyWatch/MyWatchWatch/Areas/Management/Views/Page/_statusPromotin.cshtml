@using MyWatchWatch.Models;
@{
    IQueryable<Promotion> statusPromotin = ViewBag.statusPromotin as IQueryable<Promotion>;
}
<div class="wrapper count-title d-flex">
    <div class="icon"><i class="fa fa-bullhorn"></i></div>
    <div class="name">
        <strong class="text-uppercase"><a href="@Url.Action("Index","Promotions")">Promotion</a></strong>
        <div class="count-number">@ViewBag.statusPromotin</div>
    </div>
</div>
