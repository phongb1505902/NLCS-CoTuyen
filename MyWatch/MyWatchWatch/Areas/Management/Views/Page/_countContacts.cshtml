@using MyWatchWatch.Models;
@{
    IQueryable<Contact> statusContacts = ViewBag.statusContacts as IQueryable<Contact>;
}
<div class="wrapper count-title d-flex">
    <div class="icon"><i class="fa fa-caret-square-o-up"></i></div>
    <div class="name">
        <strong class="text-uppercase"><a href="@Url.Action("Index","Contacts")">Contacts</a></strong>
        <div class="count-number">@ViewBag.statusContacts</div>
    </div>
</div>